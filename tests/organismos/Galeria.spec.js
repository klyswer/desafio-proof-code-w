import React from 'react';
/* eslint-disable no-undef */
import { shallow } from 'enzyme';
import Galeria from '../../src/front/organismos/galeria/Galeria';


describe('<Galeria />', () => {
  
  test('Verificando render de productos con store mock', () => {
    // const productos = [];
    // const wrapper = shallow(<Galeria listaProductos={productos} />);
    // expect(wrapper.find(".card_galeria").length).toBe(0);
  });
  
});
